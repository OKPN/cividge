# システム不変条件 (System Invariants) - Cividge Frontend

このドキュメントは、Cividge フロントエンド（UI / Pages）が正常に動作するために、**いかなるコード改修やリファクタリングを行っても絶対に崩してはならない性質（不変条件）** を定義したものです。

配信 Worker 側の不変条件については `cividge-kv-worker/INVARIANTS.md` を参照してください。

---

## 1. メディア表示とサムネイル描画 (Media & Thumbnail Invariants)

### [INV-FRONT-001] 動画サムネイルの `preload="metadata"` 担保
* **規則 (Rule)**:  
  ストレージ管理一覧で動画サムネイルを表示する際、`<video>` タグには必ず **`preload="metadata"`** を指定しなければならない。

* **理由 (Rationale)**:  
  ブラウザの仕様上、`preload="none"` を指定するとブラウザはメディアデータを 1 バイトも取得しないため、先頭フレームがレンダリングされずサムネイル枠が真っ黒になってしまう。サムネイル描画にはメタデータおよび先頭フレームの取得が不可欠である。

* **前提条件 (Dependency)**:  
  この先走り通信（メタデータ取得）は、Worker 側の **[INV-DELIVERY-001]（台帳存在時の 404 キャッシュ禁止 / 30秒 Grace Period）** が担保されていることで、アップロード直後でも 404 キャッシュロックを引き起こさずに安全に動作する。

* **禁止事項 (Prohibited)**:  
  - 404 を回避しようとして動画サムネイルを安易に `preload="none"` に変更すること（動画サムネイルが全滅するため）。

---

### [INV-FRONT-002] 透過的 S3 フォールバックの維持
* **規則 (Rule)**:  
  公開配信レイヤからの画像読み込みが一時的に失敗（または未伝播）した場合、`onerror` ハンドラから `loadFallbackImageFromS3` を通じて署名付き直接取得へ透過的にフォールバックしなければならない。

* **理由 (Rationale)**:  
  エッジキャッシュが準備中であっても、アップローダー本人は確実に自分のファイルを表示・確認できるようにするため。

---

## 2. リソース制限とクライアント安全性 (Resource & Client Safety)

### [INV-FRONT-003] 単一ファイルアップロード上限（128 MB 未満）
* **規則 (Rule)**:  
  ブラウザからの単一ファイルアップロード（`result.blob`）は、**厳格に 128 MB（$128 \times 1024 \times 1024$ バイト）未満に制限しなければならない**。

* **理由 (Rationale)**:  
  1. **スマホ（iOS / Android）のメモリ保護（OOM クラッシュ防止）**:  
     Cividge はブラウザ内で CID（ハッシュ）計算や EXIF 処理を行うため、ファイルサイズの約 2.5〜3 倍（128 MB の場合約 300〜380 MB）の JavaScript メモリを瞬間的に消費する。これを超える巨大ファイルを許可すると、特に iOS Safari（Jetsam 制限）等のモバイル環境でブラウザタブが強制クラッシュ（再読み込みループ）を引き起こすため。
  2. **Cloudflare エッジキャッシュの適合性**:  
     Cloudflare の単一ファイルキャッシュ上限（512 MB）に対して十分な安全マージンを確保し、全ファイルが確実にエッジキャッシュされる状態を維持するため。

* **禁止事項 (Prohibited)**:  
  - メモリ枯渇によるクラッシュリスクを無視して、安易に 500 MB や 1 GB などの巨大ファイル制限へ引き上げること。
